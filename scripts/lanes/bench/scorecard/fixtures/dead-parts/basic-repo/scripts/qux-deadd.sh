#!/usr/bin/env bash
# scripts/qux-deadd.sh - fixture: only textual reference anywhere is this
# self header comment; proves self-match exclusion (no external caller means
# DEAD, not WIRED, despite the file containing its own path).
echo dead
