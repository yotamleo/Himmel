#!/usr/bin/env bash
# scripts/relative-caller.sh - fixture: non-test, non-doc code that calls
# doc-and-code.sh by basename only (never spells the "scripts/" prefix).
echo "would call: ./doc-and-code.sh"
