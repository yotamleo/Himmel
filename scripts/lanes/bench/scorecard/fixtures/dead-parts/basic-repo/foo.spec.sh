#!/usr/bin/env bash
# foo.spec.sh - fixture: a .spec.* test file referencing spec-testonly.sh,
# which no non-test code calls (codex-3 .spec.* naming fix).
echo "would test: scripts/spec-testonly.sh"
