# test suite

Small shell test suite for a handful of utility behaviors. Run all cases with:

    bash run-tests.sh

Each case is tagged with a `TC-NNN` test-case ID in the comment above it and
in the PASS/FAIL line it prints.
