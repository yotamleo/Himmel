# toolchain

Pinned build toolchain for this repository.

- `tools/pins.env` — the canonical pin list.
- `scripts/install-tools.sh` — installs each pinned tool.
- `CHANGELOG.md` — one line per pin change.

Run `bash scripts/install-tools.sh` to install everything.
