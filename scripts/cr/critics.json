{
  "_compliance": "panel/router lanes default to per-token keys; a subscription lane is otherwise launcher-only (ToS restricts it to officially supported tools). EXCEPTION (operator-authorized 2026-07-17, HIMMEL-1096): the existing Z.ai Coding-Plan key MAY back the `glm` critic row below — the operator accepted the ToS trade-off to drop codex as the panel's single point of failure. Scope: this key + this one read-only critic row only; do NOT generalize to other subscription lanes or to router/impl dispatch without a fresh operator decision. RETIRED (2026-08-18, HIMMEL-1904): no longer exercised — the GLM subscription was cancelled and the `glm` row removed from panel below; the authorization itself is NOT revoked, re-adding the row is how it resumes. Consequence: the panel is now codex-only, restoring the single point of failure this exception was accepted to avoid — a codex outage now blocks the CR gate with no second critic.",
  "panel": [
    { "slug": "codex",      "model": "gpt-6-astra",                         "provider": "openai-codex", "route_provider": "openai-codex", "tier": "paid" }
  ]
}
