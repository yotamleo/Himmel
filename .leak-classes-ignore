# Path prefixes scripts/guardrails/leak-classes.sh --tree/--staged skips
# entirely. One prefix (or exact filename) per line, blank lines and #
# comments ignored.
#
# himmel is a single public repo now (no private/public split, no
# propagation) — this is still a general escape hatch, not a one-off: the
# one entry below is the one whole-directory vendored-code exception this
# scanner permits. Everything else that hits under --tree/--staged must be
# genuinely fixed, not added here.

# Vendored upstream code (tracked in scripts/upstreams.json), not
# hand-written here — rewriting or allow-commenting vendored third-party
# source would be lost on the next upstream sync.
templates/luna-second-brain/.obsidian/plugins/
